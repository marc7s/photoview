type AdvancedSearchResult {
  "A list of media that matched the query"
  media: [Media!]!
}

extend type Query {
  "Perform an advanced search query on the contents of the media library"
  advancedSearch(fileNames: [String], albumIDs: [Int], startDate: Time, endDate: Time, limitMedia: Int): AdvancedSearchResult!
}
